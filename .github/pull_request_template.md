## What this changes

<!-- What behaviour is different afterwards, and why. -->

## Checklist

- [ ] `dotnet build SqlCdc.slnx` is clean (warnings are errors)
- [ ] `dotnet test tests/SqlCdc.Tests` passes
- [ ] `dotnet test tests/SqlCdc.IntegrationTests` passes, or the change cannot affect it
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] Public API unchanged, or `PublicApi.approved.txt` updated and the change is intended
- [ ] No breaking change, or it is recorded under **Breaking** in the changelog
